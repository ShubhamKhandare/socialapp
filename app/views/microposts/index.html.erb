<div class="row">
	<aside class="col-md-4">
	<% if user_signed_in? %>
		<section class="user_info">
			<%= render 'shared/user_info' %>
		</section>
		<section class="stats">
    		<%= render 'shared/stats' %>
  		</section>
		<section class="micropost_form">
			<%= render 'shared/micropost_form' %>
		</section>
	<% end %>
	</aside> 
	<div class="col-md-8">
		<h3>All microposts</h3>
		<%= render 'shared/feed' %>
	</div>
</div>